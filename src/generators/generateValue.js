const generateValue = ({ type, generatorType, index }) => {
    console.log('generateAray configutype, generatorTyperation : ', type, generatorType, index);
    return { type, generatorType, index }
}

module.exports = generateValue;